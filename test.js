const chai = require('chai');

const should = chai.should();

describe("Berlin clock kata project", function () {
    context("Single minute row method", function() {
        it("demo test", function() {
            var actural = true;

            actural.should.equal(true);
        })
    })
});